[pytest]
# asyncio_mode = "auto" hace que pytest-asyncio detecte automáticamente
# los tests async y los corra en el event loop, sin necesitar @pytest.mark.asyncio
# en cada función. Más cómodo para proyectos donde casi todo es async.
asyncio_mode = auto
